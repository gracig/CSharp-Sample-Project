name: Commit
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}
    #add a comment
jobs:
  uses: "gracig/actions/.github/workflows/main.yml@main"
  with:
    WORKFLOW_TAGs: |
      dotnet/v1
