name: Commit
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}
jobs:
  main:
    uses: "gracig/actions/.github/workflows/main.yml@main"
    with:
      WORKFLOW_TAGs: |
        dotnet/v1
        container/v1
        debug
      DEPLOY_BRANCH: true
      ENV_REPO: repo
    secrets: inherit

